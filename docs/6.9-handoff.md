# 6.9 «Розширення інтерактивних тестів» — стан на момент передачі

Дата оновлення: 2026-09-16 (продовження сесії). Попередній запис цього файлу (той самий день, раніша сесія) залишав 5 пунктів прогалин як "статус невідомий" — ця сесія перевірила код проти кожного пункту й підтвердила, що всі 5 закриті реальною реалізацією та тестами (не лише заявлено).

## Гілка

`codex/6.9-interactive-tests`. Нічого не закомічено, не застаджено, не запушено — весь diff у робочому дереві (перевірено `git status --short --branch` на початку і в кінці цієї сесії).

## Результат перевірки 5 пунктів прогалин

| # | Пункт | Статус | Доказ |
| --- | --- | --- | --- |
| 1 | UI-доступ до task id 2-4 кожного формату | **DONE** | `src/modules/stage2/rounds.ts` `SQL_CATALOG` збирає id 1-4 усіх 5 форматів у один раунд (20 задач); `InteractiveFormatsShowcase.tsx` рендерить `taskPicker` — кнопка на кожну задачу раунду, усі клікабельні. Тест `rounds.test.ts:168` перевіряє, що каталог містить усі 20. |
| 2 | Durable `perBlank` після reload/retry/reveal | **DONE** | `blankTask.ts`: `perBlank` завжди детерміновано перераховується зі збереженого `submitted_json` (не з поточного запиту) — і на ідемпотентному `submit`, і на `get`. Тести `blankTask.test.ts` "DETERMINISTIC RECOVERY" (рядки 161, 192, 209). |
| 3 | `/practice/interactive` у користувацькому шляху, не лише демо | **DONE** | Додано в `DASHBOARD_NAV` (`src/constants/navigation.ts`) і сайдбар (`AppSidebar.tsx`). Раунди (`rounds.ts`) підтримують practice/diagnostic з тими самими правилами нейтральності Stage 1 (діагностика не показує correct/revealed до завершення), історію (`listRoundsAction`), "Робота над помилками" (`startMistakeRoundAction`). Стан відновлюється з БД через `getRoundAction`, не з клієнтського state. |
| 4 | Безпека/ідемпотентність окремо на кожен формат | **DONE** | Усі 5 пар дій у `src/modules/stage2/actions.ts` йдуть через однаковий ланцюжок: `requireSessionUserId()` → `withRound()` (перевірка членства завдання в раунді користувача) → домен-логіка з `FOR UPDATE`. Унікальний ключ `practice_stage2_attempts(format, task_id, user_id, round_id)` (міграція 031) не дає підмінити чужий прогрес. `expectedAttempt` захищає від стрибка на другу спробу. Гонка "завершення раунду між авторизацією і записом" покрита тестом `rounds.test.ts:155`. |
| 5 | Витік відповіді через приклад-підказку | **DONE** | Міграція `029_analogous_hint_examples.sql` — авторський `hint_example` з ІНШИМИ числовими даними для всіх 20 seed-задач (перевірено вручну по кожному рядку). `hintLadder.ts` гейтить фолбек на `explanation` (потенційно розкриває відповідь) до `retry_used === 1`. Тести `hintLadder.test.ts` рядки 13 і 74. |

Детальний розбір із номерами рядків — у `docs/mentor-tasks.md`, розділ «Таска 6.9».

## Що це означає для 6.9 в цілому

Stage 1 і Stage 2 (backend + UI) функціонально повні й покриті тестами. **Єдиний блокер** перед тим, щоб вважати 6.9 повністю готовою до продакшена — шість нових міграцій (`026`-`031`) ніколи не виконувались на жодній реальній чи тестовій базі, і повний ручний QA залогіненого користувача (клавіатура/дотик/реальний вхід) не проведений. Обидва пункти впираються в брак доступної тестової БД у цьому середовищі, не в стан коду.

## Повторний прогін валідації (ця сесія)

- `npm test`: **859 pass / 3 skip (ті самі попередні, непов'язані) / 0 fail** (862 тести).
- `npm run lint`: **0 errors**, 4 ті самі попередні непов'язані warning (`scripts/fetch-nmt-variants.mjs`, `LearningSessionsTable.tsx`).
- `npx tsc --noEmit`: **0 errors.**
- `npm run build`: успішно; `/practice/interactive` у списку маршрутів; без DB EACCES цього разу (попередній звіт згадував non-fatal EACCES під час статичної генерації діагностики — цього разу build пройшов без цього повідомлення).
- `git diff --check`: чисто, лише CRLF-попередження на існуючих файлах (пре-існуюча властивість репозиторію на Windows, не помилка).

Жодних правок коду ця сесія не робила — усі 5 пунктів вже були закриті в робочому дереві до її початку; сесія лише верифікувала це проти джерела (коду й тестів) і оновила документацію (`docs/mentor-tasks.md`, цей файл).

## Стан міграцій 026-031 (БЛОКЕР)

Жодна не виконана на живій базі. У цьому середовищі:
- `mysql`/`mysqld` — відсутні (`which` не знаходить).
- Docker Desktop встановлено, але демон не запущений (`docker ps` → `open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified`).
- `.env.local` вказує на реальний віддалений хостинг — свідомо не чіпали й не будемо.

Порядок застосування на майбутній ізольованій тестовій БД: **026 → 027 → 028 → 029 → 030 → 031** (031 обов'язково після 028, бо змінює унікальний ключ `practice_stage2_attempts`, який 028 створює).

Після підняття такої БД (Docker MySQL 8.x або локальний інстанс) — прогнати міграції по черзі, звірити `SHOW COLUMNS`/`SHOW TABLES` і кількість рядків до/після (мають збігтись, міграції лише додають), після чого виконати ручний QA-сценарій з попереднього запису цього файлу (розділ був детальний — Practice/Diagnostic/Stage 2 сценарії, перезавантаження, друге пристрій, клавіатура, мобільний viewport). Цей сценарій ще актуальний і не змінився.

## Наступні конкретні кроки

1. Підняти ізольовану тестову MySQL (Docker, коли демон стане доступний, або будь-який локальний MySQL 8.x) і застосувати міграції 026→031 за порядком вище.
2. Виконати повний ручний QA-сценарій під реальним тестовим логіном (Practice/Diagnostic/Stage 2 — усі 5 форматів, перезавантаження, друге пристрій, клавіатура, мобільний viewport).
3. Тільки після цього розглядати позначення 6.9 як повністю готової до продакшена.
4. Commit/push — лише за окремим підтвердженням користувача, не автоматично.

Секретів, токенів, credentials у цьому файлі немає.
